"use client";
import { useEffect, useState } from "react";
import api from "@/services/api";
export default function AdminProfilePage() { const [admin, setAdmin] = useState(null); const [error, setError] = useState(""); useEffect(() => { api.get("/auth/me").then((response) => setAdmin(response.data.user)).catch((err) => setError(err.response?.data?.message || "Unable to load profile.")); }, []); return <section className="mx-auto max-w-4xl px-5 py-8 lg:px-8"><h1 className="text-3xl font-bold text-slate-900">Administrator Profile</h1><p className="mt-1 text-slate-500">Account details for the LifeLink administrator.</p>{error && <p className="mt-6 rounded-lg bg-red-50 p-4 text-red-700">{error}</p>}<div className="mt-8 grid gap-5 rounded-2xl border border-slate-200 bg-white p-6 shadow-sm sm:grid-cols-2"><div><p className="text-sm text-slate-500">Name</p><p className="mt-1 text-lg font-semibold text-slate-900">{admin?.name || "Loading..."}</p></div><div><p className="text-sm text-slate-500">Email</p><p className="mt-1 text-lg font-semibold text-slate-900">{admin?.email || "Loading..."}</p></div><div><p className="text-sm text-slate-500">Role</p><p className="mt-1 text-lg font-semibold text-blue-700">Administrator</p></div><div><p className="text-sm text-slate-500">Status</p><p className="mt-1 text-lg font-semibold text-green-700">{admin?.status || "Active"}</p></div></div></section>; }
