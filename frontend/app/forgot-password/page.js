"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import BrandLogo from "../components/BrandLogo";
import api from "@/services/api";
export default function ForgotPasswordPage() { const router = useRouter(); const [email, setEmail] = useState(""); const [role, setRole] = useState("hospital"); const [message, setMessage] = useState(""); const [resetToken, setResetToken] = useState(""); const [error, setError] = useState(""); const submit = async (event) => { event.preventDefault(); setError(""); try { const response = await api.post("/auth/forgot-password", { email, role }); setMessage(response.data.message); setResetToken(response.data.resetToken || ""); } catch (err) { setError(err.response?.data?.message || "Unable to request password reset."); } }; return <main className="flex min-h-screen items-center justify-center bg-slate-50 px-5"><div className="w-full max-w-md rounded-2xl border border-slate-200 bg-white p-7 shadow-sm"><div className="mb-7 flex items-center justify-center gap-3"><BrandLogo size={48} /><span className="text-2xl font-bold text-blue-700">LifeLink</span></div><h1 className="text-2xl font-bold text-slate-900">Forgot password?</h1><p className="mt-2 text-sm text-slate-500">Request a secure 15-minute password reset link.</p><form onSubmit={submit} className="mt-6 space-y-4"><label className="block text-sm font-medium">Account type<select value={role} onChange={(e) => setRole(e.target.value)} className="mt-2 w-full rounded-lg border border-slate-300 px-3 py-2.5"><option value="hospital">Hospital</option><option value="admin">Administrator</option></select></label><label className="block text-sm font-medium">Email<input type="email" required value={email} onChange={(e) => setEmail(e.target.value)} className="mt-2 w-full rounded-lg border border-slate-300 px-3 py-2.5" /></label><button className="w-full rounded-lg bg-blue-600 px-4 py-3 font-semibold text-white hover:bg-blue-700">Request reset</button></form>{error && <p className="mt-4 rounded-lg bg-red-50 p-3 text-sm text-red-700">{error}</p>}{message && <div className="mt-4 rounded-lg bg-green-50 p-3 text-sm text-green-700"><p>{message}</p>{resetToken && <button onClick={() => router.push(`/reset-password/${resetToken}?role=${role}`)} className="mt-2 font-semibold underline">Continue to reset password</button>}</div>}<button onClick={() => router.back()} className="mt-6 text-sm font-semibold text-blue-600">← Back to login</button></div></main>; }
