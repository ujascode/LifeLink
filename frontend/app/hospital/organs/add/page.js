"use client";

import { useState } from "react";
import { useRouter } from "next/navigation";
import api from "@/services/api";

const ORGAN_TYPES = ["Heart", "Liver", "Kidney", "Lung", "Pancreas", "Intestine", "Cornea"];
const BLOOD_GROUPS = ["A+", "A-", "B+", "B-", "AB+", "AB-", "O+", "O-"];

export default function AddOrganPage() {
  const router = useRouter();
  const [form, setForm] = useState({ organType: "Heart", bloodGroup: "A+", donorAge: "", donorGender: "Male", availabilityDate: "", address: "", city: "", state: "", latitude: "", longitude: "", notes: "" });
  const [saving, setSaving] = useState(false); const [error, setError] = useState("");
  const change = (event) => setForm((current) => ({ ...current, [event.target.name]: event.target.value }));
  const submit = async (event) => { event.preventDefault(); setError(""); if (!form.donorAge || !form.availabilityDate || !form.address || !form.city || !form.state) { setError("Complete all required organ and location details."); return; } try { setSaving(true); await api.post("/organs", { organType: form.organType, bloodGroup: form.bloodGroup, donorAge: Number(form.donorAge), donorGender: form.donorGender, availabilityDate: form.availabilityDate, location: { address: form.address, city: form.city, state: form.state, latitude: form.latitude ? Number(form.latitude) : undefined, longitude: form.longitude ? Number(form.longitude) : undefined }, notes: form.notes || undefined }); router.push("/hospital/organs"); } catch (err) { setError(err.response?.data?.message || "Unable to add organ."); } finally { setSaving(false); } };
  const input = "mt-2 w-full rounded-lg border border-slate-300 px-3 py-2.5 text-slate-900 outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-100";
  return <section className="mx-auto max-w-4xl px-5 py-8 lg:px-8"><div className="mb-8"><h1 className="text-3xl font-bold text-slate-900">Add Organ</h1><p className="mt-1 text-slate-500">Register an organ for verified hospital-to-hospital exchange.</p></div>{error && <div className="mb-6 rounded-lg border border-red-200 bg-red-50 p-4 text-red-700">{error}</div>}<form onSubmit={submit} className="space-y-6 rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div className="grid gap-5 sm:grid-cols-2"><label className="text-sm font-medium text-slate-700">Organ type<select name="organType" value={form.organType} onChange={change} className={input}>{ORGAN_TYPES.map((item) => <option key={item}>{item}</option>)}</select></label><label className="text-sm font-medium text-slate-700">Blood group<select name="bloodGroup" value={form.bloodGroup} onChange={change} className={input}>{BLOOD_GROUPS.map((item) => <option key={item}>{item}</option>)}</select></label><label className="text-sm font-medium text-slate-700">Donor age<input name="donorAge" type="number" min="0" max="120" value={form.donorAge} onChange={change} className={input} required /></label><label className="text-sm font-medium text-slate-700">Donor gender<select name="donorGender" value={form.donorGender} onChange={change} className={input}><option>Male</option><option>Female</option><option>Other</option></select></label><label className="text-sm font-medium text-slate-700 sm:col-span-2">Availability date<input name="availabilityDate" type="date" value={form.availabilityDate} onChange={change} className={input} required /></label></div><div className="border-t border-slate-100 pt-6"><h2 className="font-semibold text-slate-900">Organ location</h2><div className="mt-4 grid gap-5 sm:grid-cols-2"><label className="text-sm font-medium text-slate-700 sm:col-span-2">Address<input name="address" value={form.address} onChange={change} className={input} required /></label><label className="text-sm font-medium text-slate-700">City<input name="city" value={form.city} onChange={change} className={input} required /></label><label className="text-sm font-medium text-slate-700">State<input name="state" value={form.state} onChange={change} className={input} required /></label><label className="text-sm font-medium text-slate-700">Latitude <span className="font-normal text-slate-400">(optional)</span><input name="latitude" type="number" step="any" value={form.latitude} onChange={change} className={input} /></label><label className="text-sm font-medium text-slate-700">Longitude <span className="font-normal text-slate-400">(optional)</span><input name="longitude" type="number" step="any" value={form.longitude} onChange={change} className={input} /></label></div></div><label className="block text-sm font-medium text-slate-700">Notes <span className="font-normal text-slate-400">(optional)</span><textarea name="notes" value={form.notes} onChange={change} rows="4" maxLength="1000" className={input} /></label><div className="flex flex-wrap justify-end gap-3"><button type="button" onClick={() => router.back()} className="rounded-lg border border-slate-300 px-5 py-2.5 font-semibold text-slate-700">Cancel</button><button disabled={saving} className="rounded-lg bg-blue-600 px-5 py-2.5 font-semibold text-white hover:bg-blue-700 disabled:opacity-60">{saving ? "Saving..." : "Add organ"}</button></div></form></section>;
}
