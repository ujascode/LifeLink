"use client";
import Link from "next/link";
import { useEffect, useState } from "react";
import api from "@/services/api";
export default function RequestsOverviewPage() { const [sent, setSent] = useState(0); const [received, setReceived] = useState(0); const [loading, setLoading] = useState(true); useEffect(() => { Promise.all([api.get("/organ-requests/sent"), api.get("/organ-requests/received")]).then(([a, b]) => { setSent(a.data.count ?? a.data.requests?.length ?? 0); setReceived(b.data.count ?? b.data.requests?.length ?? 0); }).finally(() => setLoading(false)); }, []); return <section className="mx-auto max-w-5xl px-5 py-8 lg:px-8"><h1 className="text-3xl font-bold text-slate-900">All Requests</h1><p className="mt-1 text-slate-500">Track requests sent by your hospital and responses received from the network.</p><div className="mt-8 grid gap-5 sm:grid-cols-2">{[["Sent requests", sent, "/hospital/requests/sent", "View sent requests"], ["Received requests", received, "/hospital/requests/received", "View received requests"]].map(([label, count, href, action]) => <Link href={href} key={href} className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm transition hover:-translate-y-0.5 hover:border-blue-300"><p className="text-sm text-slate-500">{label}</p><p className="mt-2 text-4xl font-bold text-slate-900">{loading ? "—" : count}</p><p className="mt-5 font-semibold text-blue-600">{action} →</p></Link>)}</div><Link href="/hospital/requests/new" className="mt-8 inline-flex rounded-xl bg-blue-600 px-5 py-3 font-semibold text-white hover:bg-blue-700">Create a new request</Link></section>; }
