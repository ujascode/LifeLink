"use client";
import { useEffect, useState } from "react";
import { useParams, useRouter } from "next/navigation";
import api from "@/services/api";
export default function AdminRequestDetailsPage() { const { id } = useParams(); const router = useRouter(); const [request, setRequest] = useState(null); const [error, setError] = useState(""); useEffect(() => { if (id) api.get(`/admin/requests/${id}`).then((response) => setRequest(response.data.request)).catch((err) => setError(err.response?.data?.message || "Unable to load request.")); }, [id]); if (error) return <section className="mx-auto max-w-3xl px-5 py-8"><p className="rounded-lg bg-red-50 p-4 text-red-700">{error}</p></section>; if (!request) return <div className="p-8 text-slate-500">Loading request...</div>; return <section className="mx-auto max-w-4xl px-5 py-8 lg:px-8"><button onClick={() => router.back()} className="text-sm font-semibold text-blue-600">← Back</button><div className="mt-5 rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex flex-wrap items-start justify-between gap-4"><div><h1 className="text-3xl font-bold text-slate-900">{request.organ?.organType || "Organ"} request</h1><p className="mt-1 text-sm text-slate-500">{request._id}</p></div><span className="rounded-full bg-blue-50 px-3 py-1 text-sm font-semibold text-blue-700">{request.status}</span></div><dl className="mt-8 grid gap-6 sm:grid-cols-2"><div><dt className="text-sm text-slate-500">Patient</dt><dd className="mt-1 font-semibold">{request.patientName}, {request.patientAge} years</dd></div><div><dt className="text-sm text-slate-500">Urgency</dt><dd className="mt-1 font-semibold">{request.urgency}</dd></div><div><dt className="text-sm text-slate-500">Requesting hospital</dt><dd className="mt-1 font-semibold">{request.requestingHospital?.hospitalName || "—"}</dd></div><div><dt className="text-sm text-slate-500">Supplying hospital</dt><dd className="mt-1 font-semibold">{request.supplyingHospital?.hospitalName || "—"}</dd></div><div className="sm:col-span-2"><dt className="text-sm text-slate-500">Reason</dt><dd className="mt-1 leading-7 text-slate-700">{request.reason}</dd></div></dl></div></section>; }
